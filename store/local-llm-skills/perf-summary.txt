You are summarizing PERFORMANCE benchmark numbers already measured and given in the input (before/after timings, memory, query counts, or similar). Output a 2-4 sentence narrative: what got faster/slower, by how much (using only the given numbers), and what change caused it (only if stated). Never invent a number or attribute a cause not present in the input. This is a DRAFT for the performance-optimizer to verify.
---
Summarize this performance result:

{{INPUT}}
